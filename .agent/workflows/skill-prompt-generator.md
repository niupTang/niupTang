---
description: 基于Skills的智能提示词生成系统，支持人像、设计、艺术、产品等领域的AI提示词生成
---

# Skill Prompt Generator - 智能提示词生成系统

这是一个基于专家知识库的智能提示词生成系统，能够根据用户的意图，自动调用对应的专家模型，生成高质量的 AI 图像提示词。

## 🎯 核心领域

支持以下 7 大核心领域的提示词生成：
- **portrait** (人像摄影) - 包含 500+ 个专业元素
- **design** (平面设计) - 支持 Bento Grid, Apple 风格等模板
- **art** (艺术绘画) - 支持水墨、油画、插画等多种风格
- **product** (产品摄影) - 专为电商和广告设计的商业摄影
- **video** (视频生成) - 针对视频生成模型油化的动态描述
- **interior** (室内设计) - 建筑与空间设计
- **common** (通用摄影) - 镜头、光影等基础技术支持

## 🛠️ 如何使用

当用户需要生成高质量 AI 提示词时，请按照以下步骤操作：

### 步骤 1：识别用户意图
分析用户的原始请求，确定所属领域（如“生成一个亚洲女孩”属于 `portrait`）。

### 步骤 2：调用生成引擎
进入 `.agent/skill-prompt-generator` 目录，使用 Python 引擎生成提示词。

```bash
python .agent/skill-prompt-generator/intelligent_generator.py --intent "<用户的原始需求>"
```

或者使用交互式模式（如果需要更精准的控制）：

```python
from .agent.skill_prompt_generator.intelligent_generator import IntelligentGenerator

gen = IntelligentGenerator()
prompt = gen.generate_from_intent({"subject": "...", "style": "..."})
print(prompt)
```

### 步骤 3：获取结果
引擎会自动基于 `prompt_framework.yaml` 框架和 `elements.db` 数据库，生成包含主体、构图、光影、镜头参数等全方位的提示词。

---

## 🏗️ 项目结构参考

- **引擎核心**: `.agent/skill-prompt-generator/intelligent_generator.py`
- **框架定义**: `.agent/skill-prompt-generator/prompt_framework.yaml`
- **元素数据库**: `.agent/skill-prompt-generator/extracted_results/elements.db`
- **专家技能层**: `.agent/skill-prompt-generator/.claude/skills/`

## 💡 进阶技巧
- **小作坊下料猛**: 如果有特定的行业经验，可以手动在 `intelligent_generator.py` 中添加自定义参数。
- **模板优先**: 对于海报或 PPT 设计，优先搜索 `design` 领域的模板库。
